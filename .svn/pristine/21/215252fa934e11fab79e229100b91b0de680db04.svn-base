import styled from 'styled-components';
import {Button, Input} from 'antd';

const {TextArea} = Input;
const theme = {
  addBtnMarginTop: '16px',
  addBtnMarginBottom: '42px',
}

const CommentDiv = styled.div`
  flex: auto;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
`;

const AddButton = styled(Button)`
  margin-top: ${theme.addBtnMarginTop};
  //margin-bottom: ${theme.addBtnMarginBottom};
`;
const CommentTextArea = styled(TextArea)`
  min-height: 280px;
  resize: none;
  flex:auto;
`;
module.exports = {
  CommentDiv,
  AddButton,
  CommentTextArea,
  theme,
}
