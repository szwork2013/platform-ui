import styled from 'styled-components';

//皮肤定义
const theme = {
	fontSize: 14,
	minHeight: 24,
	maxHeight: 38,
}

const StyledCellDiv = styled.div`
	height: 100%;
	textarea {
		width: 95%;
		font-size: ${theme.fontSize}px;
		min-height: ${theme.minHeight}px !important;
		max-height: ${theme.maxHeight}px !important;		
	}
	input, select {
		width: 95%;
		font-size: ${theme.fontSize}px;
		min-height: ${theme.minHeight+6}px !important;
		max-height: ${theme.maxHeight}px !important;		
	}
	div {
		width: 100%;
		min-height: ${theme.minHeight}px !important;
	}
	.ant-select {
		width: 100% !important;
	}
`;

const StyledDisplayDiv = styled.div`
	width: 100%;
		min-height: ${theme.minHeight}px;
`;

export {
	StyledCellDiv,
	StyledDisplayDiv,
  theme
}
