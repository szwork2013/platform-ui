import styled from 'styled-components';
import { Upload } from 'antd'

//样式定义
const theme = {
}

const StyledUpload = styled(Upload)`
  .ant-upload-select{
  	float:${(props)=>props.layout=='inline'?'right !important':undefined};
  }
`;

export {
  StyledUpload,
}
