import {ModuleLayout} from 'components';

const menuItems = [
  {
    path: 'list',
    icon: 'execution',
    title: '立项数据清理'
  }
];

const Layout = (props) => {
  return (<ModuleLayout
    trigger
    siderTitle='数据清理'
    siderIcon='solution'
    menu={menuItems}
    {...props}/>)
}

export default Layout;
