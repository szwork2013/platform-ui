import styled from 'styled-components';

//样式定义
const theme = {
	hasActionBar: true,
	hasPaginationBar: true,
	fontSize: 14,
}

const StyledViewDiv = styled.div`
	width: 100%;
	height: 100%;
	overflow: hidden;
`;

export {
	StyledViewDiv,
	theme
}
